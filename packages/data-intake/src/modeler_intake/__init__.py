"""Client data intake.

Flow: store the untouched file in the raw vault -> read it into a grid that keeps every cell reference ->
apply a confirmed mapping recipe -> validate -> canonical records that point back to their source cells.
"""
