[project]
name = "modeler-contracts"
version = "0.1.0"
description = "Payload dataclasses shared by the API, Temporal workflows, and engine workers"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/modeler_contracts"]
